# Reports Folder

This folder stores local JSON outputs from long-context tests.
They are ignored by git by default (see .gitignore).

Example files:
- long_test_8k.json
- long_test_batch_4k_gpu.json
